{
  "name": "wager-services",
  "workspaces": [
    "api",
    "data",
    "scraper"
  ],
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "tsc --build",
    "watch": "tsc --build --watch"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}