{
  "name": "wager-ws",
  "scripts": {
    "build": "tsc --build",
    "dev": "nodemon --config nodemon.json src/index.ts",
    "dev:debug": "nodemon --config nodemon.json --inspect-brk src/index.ts",
    "start": "cd dist && pm2 start index.js --name wager-ws"
  },
  "dependencies": {
    "messenger": "^0.0.9",
    "ws": "^8.5.0"
  },
  "devDependencies": {
    "@types/messenger": "^1.4.29",
    "@types/node-ipc": "^9.2.0",
    "@types/ws": "^8.2.2"
  }
}
